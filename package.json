{
  "name": "vex-cad",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-assets.mjs",
    "parts": "python tools/build_parts.py --out public/parts"
  },
  "engines": { "node": ">=22" }
}
